# Server
PORT=3001

# Database
DB_PATH=./data/synapse.db

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

CANVAS_BASE_URL=
CANVAS_ACCESS_TOKEN=

# Mastery thresholds (0..1)
PG_LEARNING_THRESHOLD=0.55
PG_KNOWN_THRESHOLD=0.78

# Optional: limit nodes in PG snapshot for readability
PG_MAX_NODES=250

# Gemini
GEMINI_API_KEY=your_key_here
GEMINI_EMBED_MODEL=text-embedding-004   # default if unset

# Embedding alignments
EMBED_SIM_THRESHOLD=0.82                # cosine threshold
EMBED_TOPK=3                            # per concept, per other graph